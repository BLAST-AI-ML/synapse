# Install latest dependencies with
#   conda create -n gui -f gui-base.yml
#
# But we usually create a lockfile from this file:
#   conda-lock --file gui-base.yml --lockfile gui-lock.yml
#
name: gui
channels:
  - conda-forge
  - pytorch
dependencies:
  - botorch==0.11.0
  - cpuonly
  - matplotlib
  - pandas
  - pip
  - plotly<6
  - pymongo
  - python<3.13
  - pytorch
  - pyyaml
  - scipy
  - torchaudio
  - torchvision
  - trame
  - trame-plotly
  - trame-router
  - trame-vuetify
  - pip:
    - pywebview[qt]
    - sfapi_client
    - git+https://github.com/slaclab/lume-model.git@079a758db388487b5d211bef245f74f025c384ab
